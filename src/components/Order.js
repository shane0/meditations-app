/* eslint-disable react/prop-types */ // TODO: upgrade to latest eslint tooling
import React from "react";

const Sequence = (props) => {
  return <p>session meditation order: {props.order}</p>;
};

export default Sequence;
